# Copy this file to `.env` and fill in the values for your environment.

# Required: URL for the telemetry batch endpoint.
COLORBUDDY_TELEMETRY_ENDPOINT=https://localhost/colorbuddy/metrics

# Optional: API key header authentication (default header is x-api-key).
COLORBUDDY_TELEMETRY_API_KEY=replace-with-api-key
# COLORBUDDY_TELEMETRY_API_KEY_HEADER=x-api-key

# Optional: HTTP Basic authentication; if both values are present they override the API key.
# COLORBUDDY_TELEMETRY_USERNAME=service-account
# COLORBUDDY_TELEMETRY_PASSWORD=replace-with-password

#include <string>
#include <iostream>

int main() {
  std::string classes = "bg-primary hover:bg-primary focus:text-accent text-accent from-primary via-accent to-accent";
  std::cout << classes;
  return 0;
}
